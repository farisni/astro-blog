# Contributing

感谢你帮助改进 Astro Cleanfit。

## 开始开发

1. Fork 并克隆仓库。
2. 运行 `npm install`。
3. 运行 `npm run dev`。
4. 提交前运行 `npm run check` 和 `npm run build`。

## 贡献边界

- 保持主题为纯静态 Astro 项目。
- 新增客户端 JavaScript 前，优先考虑 HTML 和 CSS 是否足够。
- 不提交真实个人信息、密钥或没有明确再分发许可的素材。
- UI 修改需要同时检查桌面端、平板和手机。

提交 Pull Request 时，请说明改动目的、测试方式，以及涉及视觉变化时的前后截图。
